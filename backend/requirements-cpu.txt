# CPU-only dependency set for the bundled installer.
# IMPORTANT — version pins matter here:
#   torch==2.6.0 + torchaudio==2.6.0       (matches v1; pyannote 3.3.2 compat)
#   speechbrain==1.0.3                     (1.1.0 introduces broken lazy imports)
#   pytorch-lightning==2.2.5 + lightning==2.2.5
#                                          (2.6.x adds inspect.stack() call that
#                                          recurses infinitely with speechbrain's
#                                          LazyModule on Python embeddable
#                                          distributions — see v2.1.4 changelog)
#   numpy==2.1.3                           (works with torch 2.6)
#
# Usage:
#   python setup.py   (creates backend/.venv)
#   - or -
#   .\build-bundle.ps1   (creates the portable bundle that ships with installer)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
anthropic
python-dotenv
sounddevice
soundfile
scipy
faster-whisper
huggingface_hub==0.23.0
pyannote.audio==3.3.2
speechbrain==1.0.3
pytorch-lightning==2.2.5
lightning==2.2.5
pywin32
pyaudiowpatch
numpy==2.1.3
