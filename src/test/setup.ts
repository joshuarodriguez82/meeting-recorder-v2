// Extends `expect` with the DOM matchers (toBeInTheDocument,
// toHaveTextContent, …) and registers RTL's automatic cleanup between
// tests. Loaded by vitest.config.ts's setupFiles.
import "@testing-library/jest-dom/vitest";
