{
  "name": "meeting-recorder-mobile",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Android companion app for Meeting Recorder — on-the-go mic recording that syncs into the same OneDrive folder the desktop app processes.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "android:overlay": "node scripts/apply-android-overlay.mjs",
    "android:apk": "npm run build && npm run cap:add && npm run cap:sync && npm run android:overlay && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
